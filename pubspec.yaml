name: taoss3932_web_site
description: five_bell_hyun portfolio

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.4+41

environment:
  sdk: ">=3.4.1 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  url_launcher: ^6.1.6
  google_fonts: ^5.1.0
  carousel_slider: ^4.1.1
  animated_text_kit: ^4.2.1

  #web
  url_strategy: ^0.2.0 #링크에 샵 삭제
  seo_renderer: ^0.6.0 #seo rendering

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:

  uses-material-design: true
  assets:
    - assets/
    - assets/images/
    - repo/